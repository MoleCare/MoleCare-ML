version: '3'

services: 
  tensorflow-serving:
    build: 
      context: ./
      dockerfile: Dockerfile.serving
    command:
      - '--model_config_file=/models/models.conf'
    ports:
      - 8501:8501

volumes:
  sql_data: