# Security floors for TRANSITIVE dependencies.
#
# These packages are not imported directly by MoleCare-ML -- they arrive via
# tensorflow, wandb, metaflow, boto3 and friends. Each floor here is the first
# version that clears a known advisory. A constraint only applies if the
# package is actually pulled into the tree, so nothing is installed that the
# resolver would not have installed anyway.
#
# Applied via: uv pip compile requirements.txt -c constraints.txt ...

certifi>=2024.7.4
cryptography>=50.0.0
filelock>=3.20.3
fonttools>=4.60.2
h11>=0.16.0
httplib2>=0.32.0
idna>=3.15
Jinja2>=3.1.6
Markdown>=3.8.1
MarkupSafe>=3.0.3
marshmallow>=3.26.2
oauthlib>=3.2.2
protobuf>=7.36.0
pyasn1>=0.6.4
PyNaCl>=1.6.2
requests>=2.34.2
setuptools>=83.0.0
soupsieve>=2.8.4
urllib3>=2.7.0
virtualenv>=20.36.1
wheel>=0.38.1
